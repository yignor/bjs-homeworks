// task 1
console.clear();

const weapons = [new Knife(), new Staff(), new Axe(), new StormStaff(), new LongBow(), new Bow()];

// const task1 = weapons.find(getNames => getNames.name != null);
// console.log(task1)

function getNames(arr) {
    if (getNames.name != null) {
        return getNames.name;
    } else {
        return 'Errore';
    };
};

console.log(getNames(weapons))
// console.log(1)
// function getNames(arr) {
//     return arr.every(arr.name != null);
// };
// console.log(getNames(weapons))

// function getCountReliableWeapons(arr) {
    //     return arr.
    // }